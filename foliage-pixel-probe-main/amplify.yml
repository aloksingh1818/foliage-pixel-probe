version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18
        - cd foliage-pixel-probe-main
        - npm ci --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: foliage-pixel-probe-main/dist
    files:
      - '**/*'
  cache:
    paths:
      - foliage-pixel-probe-main/node_modules/**/*
  customHeaders:
    - pattern: '**/*'
      headers:
        - key: 'Cache-Control'
          value: 'public, max-age=0, must-revalidate' 